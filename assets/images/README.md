Place your actual PNG logo here as `logo.png` or `logo-ra.png` to be used by the site.

- `help-africa-now.png` and `logo-ra.svg` are available; `help-africa-now.png` is used as the primary navbar logo (fallback to `logo-ra.svg`).
- If you want to use the PNG you provided, drop it into this folder and name it `logo.png`. The navbar will continue to display the SVG by default; change the `src` in the HTML if you'd like to force the PNG.

If you want me to import the PNG you uploaded to the chat into this folder, say "import logo" and I'll add it as `assets/images/logo.png` and attempt to auto-tint it to the platform colors.